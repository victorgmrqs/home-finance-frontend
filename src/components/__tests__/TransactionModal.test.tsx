import { describe, it, expect } from 'vitest'
import { TransactionModal } from '@/components/TransactionModal'

describe('TransactionModal', () => {
  it('should be a function', () => {
    expect(typeof TransactionModal).toBe('function')
  })
})
